stages:
  - build
  - deploy

deploy-job:
  stage: deploy
  script:
    - echo "🚀 Starting deployment"
    - ls -l
    - cat deploymentrepo/myapp-deployment.yaml
    - echo "✅ Deployment simulation complete"

