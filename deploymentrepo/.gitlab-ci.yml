stages:
  - deploy

deploy_to_k8s:
  stage: deploy
  script:
    - echo "🚀 Starting deployment"
    - cat deploymentrepo/myapp-deployment.yaml  # optional: show content
    - echo "✅ YAML ready for deployment"
  tags:
    - shell
